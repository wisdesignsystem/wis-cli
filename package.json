{
  "name": "cli",
  "description": "An React Micro App Build Toolkit",
  "keywords": [
    "react",
    "react-router",
    "webpack",
    "module-federation",
    "micro-frontends",
    "build-tool"
  ],
  "type": "module",
  "scripts": {
    "prepare": "husky",
    "preinstall": "npx only-allow pnpm",
    "changelog": "pnpm changeset",
    "version": "pnpm changeset version && node ./scripts/version.js",
    "alpha": "pnpm changeset pre enter alpha && pnpm run version && pnpm changeset pre exit",
    "beta": "pnpm changeset pre enter beta && pnpm run version && pnpm changeset pre exit",
    "rc": "pnpm changeset pre enter rc && pnpm run version && pnpm changeset pre exit",
    "publish": "pnpm changeset tag && git push --follow-tags && pnpm publish -r",
    "format": "biome check --write",
    "clean": "rm -rf node_modules **/*/node_modules"
  },
  "lint-staged": {
    "**/*.{js,jsx,json}": "biome check --write"
  },
  "devDependencies": {
    "@changesets/cli": "2.27.5",
    "@commitlint/cli": "18.6.1",
    "@commitlint/config-conventional": "18.6.3",
    "@biomejs/biome": "1.9.4",
    "husky": "9.0.11",
    "lint-staged": "15.2.7"
  }
}
